<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Support</title>
        <link rel="stylesheet" type="text/css" href="../css/styles.css">
        <style>
         body {
    font-family: Arial, sans-serif;
    background-color: #170b41;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    display: flex;
    justify-content: center;
    align-items: flex-start; /* Align items at the top */
    min-height: 50%; /* Ensure the body covers the full height of the viewport */
    padding-bottom: 50px; /* Increase distance from the footer */
}

.container {
    max-width: 300px;
    padding: 20px;
    border-radius: 10px;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

.container h2 {
    text-align: center;
    color: #170b41;
}

.form-group {
    margin-bottom: 15px;
}

label {
    font-weight: bold;
}

input[type="email"],
input[type="password"] {
    width: 100%;
    padding: 10px;
    border-radius: 5px;
    border: 1px solid #ccc;
    box-sizing: border-box;
}

input[type="submit"] {
    width: 100%;
    padding: 10px;
    border: none;
    border-radius: 5px;
    background-color: #170b41;
    color: white;
    cursor: pointer;
    font-size: 16px;
    margin-top: 10px;
}

input[type="submit"]:hover {
    background-color: #0e0621;
}

.into {
    text-align: center;
    color: white;
    margin-bottom: 20px;
    margin-top: 0; /* Adjust the top margin of the .into class */
}


        </style>
    </head>
<body>
    <header class="headers">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
        <%- include('../common/header')%>
    </header>

    <main class="main">
        <div class="into">
            <p>Welcome to SolutionWebsPro! Your gateway to innovative</p>
            <p>tech solutions and bespoke web development services.</p>
            <p>Join our vibrant community to unlock the power of cutting-edge technology,</p>
            <p>drive your projects forward, and bring your ideas to life. Whether you're</p>
            <p>here to sign up or log in, we're dedicated to empowering your digital journey</p>
            <p>with top-notch solutions tailored to your needs. Let's build something remarkable together.</p>
        </div>

        <div class="container">
            <h2>Login</h2>
            <form id="loginForm" onsubmit="return validateForm()">
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" required>
                </div>

                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>

                <input type="submit" value="Login" onclick="openLogin()">
                <input type="submit" value="Signup" onclick="openSignup()">
            </form>
        </div>

        <script>
            function openSignup() {
                window.location.href = "signup.html";
            }
               
            function openLogin() {
                var email = document.getElementById("email").value;
                var password = document.getElementById("password").value;
                if (email === "" || password === "") {
                    alert("Please enter both email and password for login!");
                    return false;
                }

                // Simulated user database
                var users = {
                    "user1@example.com": "password@1",
                    "user2@example.com": "password@2",
                    "user3@example.com": "password@3"
                };

                // Check if the email exists in the database and if the password matches
                if (users.hasOwnProperty(email)) {
                    if (users[email] === password) {
                        // Password matches, redirect to index.html
                        window.location.href = "index.html";
                    } else {
                        alert("Incorrect password for the provided email!");
                    }
                } else {
                    alert("Email not found!");
                }
            }

            function validateForm() {
                var email = document.getElementById("email").value.toLowerCase();
                var password = document.getElementById("password").value;

                var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                if (!emailRegex.test(email)) {
                    alert("Invalid email format");
                    return false;
                }

                var passwordRegex = /^(?=.*[A-Z])(?=.*\d)(?=.*[!@#$%^&*()_+])[A-Za-z\d!@#$%^&*()_+]{8,}$/;
                if (!passwordRegex.test(password)) {
                    alert("Password should have at least one uppercase character, one number, one special character, and be at least 8 characters long");
                    return false;
                }

                return true;
            }
        </script>
    </main>

    <footer class="footers">
        <%- include('../common/footer')%>
    </footer>
</body>
</html>
